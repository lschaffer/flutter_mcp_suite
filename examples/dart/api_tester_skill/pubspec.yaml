name: mcp_api_tester_skill
description: Pure Dart agent skill for live API health probing, schema validation, and diagnostic reporting.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  dart_mcp_core:
    path: ../../../dart_mcp_core
  http: ^1.6.0
  path: ^1.9.1

dependency_overrides:
  dart_mcp_core:
    path: ../../../dart_mcp_core

dev_dependencies:
  lints: ^6.0.0
