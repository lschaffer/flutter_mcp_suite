name: mcp_sqlite_analyst_skill
description: Pure Dart agent skill that inspects database schemas, runs analytical SQL queries, and generates data reports.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  dart_mcp_core:
    path: ../../../dart_mcp_core
  path: ^1.9.1

dependency_overrides:
  dart_mcp_core:
    path: ../../../dart_mcp_core

dev_dependencies:
  lints: ^6.0.0
